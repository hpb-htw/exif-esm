export namespace EXIF {
    const getData = async (url: any): Promise<any> => {
        return undefined;
    };

    const getTag = (img:any, tag:string): any => {
        return undefined;
    }

    const getAllTags = (img: any):any => {
        return undefined;
    }

    const pretty = (img:any):string => {
        return "";
    }

    const readFromBinaryFile = (file:any):any => {
        return undefined;
    }
}
